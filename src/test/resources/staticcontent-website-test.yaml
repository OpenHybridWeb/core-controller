apiVersion: 1

envs:
  - name: test
  - name: prod

components:
  - context: /test-only-dev
    kind: git
    spec:
      url: giturl1
      dir: /subidr
      envs:
        - test: special-branch
  # should be ignored in test env
  - context: /test-ignored
    kind: git
    spec:
      url: giturl2
      envs:
        - prod: onlyprodbranch
  # homepage
  - context: /
    kind: git
    spec:
      url: giturl2
