apiVersion: 1
defaults:
  envs: [ test, dev, stage, prod ]

components:
  - context: /test-only-dev
    kind: git
    spec:
      url: giturl1
      dir: /subidr
      envs:
        - test: special-branch
  # homepage
  - context: /
    kind: git
    spec:
      url: giturl2
